server {
    listen 80;

    location /storage/avatars/ {
        alias /storage/avatars/;
    }

    location /static/ {
        alias /app/staticfiles/;
    }

    location /storage/ {
        return 404;
    }
}
